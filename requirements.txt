# Dipendenze Python per il progetto active_digital_twin_5g
# Ambiente conda testato: sionna2  |  Python 3.10  |  CUDA 12.x
#
# Setup rapido:
#   conda create -n sionna2 python=3.10
#   conda activate sionna2
#   pip install -r requirements_sionna2.txt
#
# Nota: i pacchetti Jupyter/IPython (ipykernel, ipython, jupyter_client, ecc.)
# vengono gestiti da conda automaticamente quando si aggiunge il kernel al
# notebook. Non è necessario installarli via pip.

# ── Deep Learning ─────────────────────────────────────────────────────────────
tensorflow==2.20.0
keras==3.11.3          # incluso in tensorflow, pin esplicito per riproducibilità

# ── CUDA — installate automaticamente come dipendenze di tensorflow su pip ────
# Necessarie per il supporto GPU quando si installa TF via pip (non via conda).
# Se si usa conda per gestire CUDA, queste righe possono essere omesse.
nvidia-cublas-cu12==12.9.1.4
nvidia-cuda-cupti-cu12==12.9.79
nvidia-cuda-nvcc-cu12==12.9.86
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cudnn-cu12==9.13.0.50
nvidia-cufft-cu12==11.4.1.4
nvidia-curand-cu12==10.3.10.19
nvidia-cusolver-cu12==11.7.5.82
nvidia-cusparse-cu12==12.5.10.65
nvidia-nccl-cu12==2.28.3
nvidia-nvjitlink-cu12==12.9.86

# ── Dati e ML ─────────────────────────────────────────────────────────────────
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
h5py==3.14.0            # salvataggio modelli Keras (.h5)

# ── Visualizzazione ───────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2

# ── Dipendenze transitive di TensorFlow (installate automaticamente) ──────────
# Elencate esplicitamente per fissare le versioni testate e garantire
# la riproducibilità in caso di aggiornamenti upstream.
absl-py==2.3.1
astunparse==1.6.3
flatbuffers==25.2.10
gast==0.6.0
google-pasta==0.2.0
grpcio==1.75.0
libclang==18.1.1
Markdown==3.9
ml_dtypes==0.5.3
namex==0.1.0
opt_einsum==3.4.0
optree==0.17.0
protobuf==6.32.1
tensorboard==2.20.0
tensorboard-data-server==0.7.2
termcolor==3.1.0
wrapt==1.17.3
Werkzeug==3.1.3

# ── Utilities generali ────────────────────────────────────────────────────────
requests==2.32.5
urllib3==2.5.0
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10
pillow==11.3.0
packaging==25.0
pytz==2026.2
tzdata==2026.2
joblib==1.5.3
threadpoolctl==3.6.0
rich==14.1.0
